AlphaDog v1.3.55 - Admin No-PIN Bridge Stable

Control/scheduled worker route retained:
POST /main-ui/admin-refresh/start
GET /main-ui/admin-refresh/status

This is still the backend runner. Main UI only triggers it.
