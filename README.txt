AlphaDog Control Room Label + Manual SQL Fix

Fixes:
- Rebuilt SCRAPE grid so no blank/no-label buttons remain.
- Kept Run Derived Metrics and MLB Players G1-G6 inside the SCRAPE grid.
- Removed orphan bottom scrape buttons.
- Made Manual SQL textarea editable on iPhone/Safari.
- Added Clear SQL and Select SQL buttons next to Run SQL.

No migration required.
Do not replace config.txt.
